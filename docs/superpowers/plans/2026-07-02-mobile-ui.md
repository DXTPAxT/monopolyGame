# Mobile UI (Giao diện điện thoại) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Làm cho Cờ Tỷ Phú 3D chơi tốt trên điện thoại (dọc là chính, có hỗ trợ ngang) mà **không đổi gì ở layout desktop hiện tại**.

**Architecture:** Một codebase responsive. Nguồn sự thật cho JavaScript là hook `useViewport()` (dựa trên `window.matchMedia`); layout DOM thuần dùng class `md:` của Tailwind. Bàn cờ vẫn là 3D (react-three-fiber) nhưng camera tự thích ứng khung dọc; HUD/nút/modal trên mobile được kéo ra khỏi mặt bàn thành thanh/tấm trượt DOM dính đáy; ba panel phụ (Người chơi / Tài sản / Chat) mở qua tab đáy + drawer trượt lên.

**Tech Stack:** React 19, TypeScript, Tailwind CSS 3.4, react-three-fiber 9 / drei 10 / three 0.184, Vite 5, Vitest 3 (môi trường `node`).

## Global Constraints

- **Desktop bất biến:** với chiều rộng ≥ 768px, mọi màn hình phải render **y hệt hiện tại**. Mọi thay đổi mobile phải nằm sau điều kiện `isMobile` (JS) hoặc class `max-md:` / khối `md:hidden` / `md:block` (CSS).
- **Breakpoint mobile:** chiều rộng `< 768px` (điểm `md` mặc định của Tailwind).
- **Không đổi** engine, socket, luật chơi, hay đường đi dữ liệu game. Chỉ đụng frontend UI.
- **Vitest:** môi trường `node`, chỉ include `src/**/*.test.ts` (KHÔNG test `.tsx`). Không có jsdom / @testing-library. → Chỉ viết unit test cho **logic thuần**; component hình ảnh nghiệm thu bằng `npx tsc -b` (exit 0) + `npx vite build` (exit 0) + test tay DevTools.
- **Thư mục làm việc test/build:** `cd frontend` trước khi chạy. Frontend cài deps cần `--legacy-peer-deps`.
- **Tap target** ≥ 44×44px cho nút mobile; input `font-size ≥ 16px` (dùng `text-base`) để iOS không auto-zoom.
- **A11y (giữ chuẩn dự án):** phần tử overlay/drawer/modal mới phải có `role="dialog"` + `aria-modal="true"` + đóng bằng phím `Escape` + `aria-label` cho nút icon.
- **Ngôn ngữ UI:** tiếng Việt, khớp giọng văn hiện có.
- Mỗi task kết thúc phải **xanh** `npx tsc -b` và `npx vite build` trước khi commit.

**Nguồn:** Spec `docs/specs/2026-07-02-mobile-ui-design.md`.

---

## File Structure

**Tạo mới:**
- `frontend/src/hooks/useViewport.ts` — hook + hàm thuần `computeViewport`. Nguồn sự thật viewport cho JS.
- `frontend/src/hooks/useViewport.test.ts` — unit test cho `computeViewport`.
- `frontend/src/components/mobile/BottomSheet.tsx` — primitive tấm trượt lên (dùng cho cả drawer panel lẫn modal HUD mobile).
- `frontend/src/components/mobile/MobileNavBar.tsx` — thanh tab đáy 3 nút mở drawer.

**Sửa:**
- `frontend/index.html` — thêm `viewport-fit=cover`.
- `frontend/src/index.css` — util safe-area + `touch-action` + keyframe slide.
- `frontend/src/App.tsx` — gọi `useViewport`; nhánh mobile: ẩn sidebar, thêm nav đáy + drawer; header mảnh; toast lỗi.
- `frontend/src/components/scene3d/GameBoard3D.tsx` — thêm `<ResponsiveCamera>`; guard `<Html>` HUD.
- `frontend/src/components/Board.tsx` — trên mobile không render HUD-trên-bàn; render HUD DOM dính đáy (dùng `BottomSheet` cho modal).
- `frontend/src/components/GameLobby.tsx` — responsive 1 cột.
- `frontend/src/components/modals/WinnerModal.tsx` — co theo màn nhỏ.
- `frontend/src/components/HelpButton.tsx` — modal full-width mobile.

**Nhánh git:** `feature/mobile-ui` (đã tạo, đã có commit spec). Làm tiếp trên nhánh này.

---

## Task 1: Hook `useViewport`

**Files:**
- Create: `frontend/src/hooks/useViewport.ts`
- Test: `frontend/src/hooks/useViewport.test.ts`

**Interfaces:**
- Consumes: nothing.
- Produces:
  - `export interface Viewport { width: number; isMobile: boolean; isPortrait: boolean; isLandscape: boolean }`
  - `export function computeViewport(width: number, portrait: boolean): Viewport`
  - `export function useViewport(): Viewport`

- [ ] **Step 1: Viết test thất bại** — `frontend/src/hooks/useViewport.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { computeViewport } from './useViewport';

describe('computeViewport', () => {
  it('điện thoại dọc: hẹp + portrait', () => {
    const vp = computeViewport(390, true);
    expect(vp.isMobile).toBe(true);
    expect(vp.isPortrait).toBe(true);
    expect(vp.isLandscape).toBe(false);
    expect(vp.width).toBe(390);
  });

  it('ngưỡng 768 là desktop (không mobile)', () => {
    expect(computeViewport(768, false).isMobile).toBe(false);
  });

  it('767 là mobile', () => {
    expect(computeViewport(767, false).isMobile).toBe(true);
  });

  it('landscape là nghịch đảo của portrait', () => {
    expect(computeViewport(900, false).isLandscape).toBe(true);
    expect(computeViewport(900, true).isLandscape).toBe(false);
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận FAIL**

Run: `cd frontend && npx vitest run src/hooks/useViewport.test.ts`
Expected: FAIL — `Failed to resolve import './useViewport'` hoặc `computeViewport is not a function`.

- [ ] **Step 3: Viết implementation** — `frontend/src/hooks/useViewport.ts`

```ts
import { useState, useEffect } from 'react';

export interface Viewport {
  width: number;
  isMobile: boolean;
  isPortrait: boolean;
  isLandscape: boolean;
}

/** Hàm thuần (test được ở môi trường node): suy ra cờ viewport từ width + orientation. */
export function computeViewport(width: number, portrait: boolean): Viewport {
  const isMobile = width < 768;
  return { width, isMobile, isPortrait: portrait, isLandscape: !portrait };
}

function readViewport(): Viewport {
  if (typeof window === 'undefined') {
    // Mặc định desktop khi không có window (an toàn cho test/SSR).
    return computeViewport(1280, false);
  }
  const portrait = window.matchMedia('(orientation: portrait)').matches;
  return computeViewport(window.innerWidth, portrait);
}

/** Hook React: theo dõi thay đổi kích thước & hướng màn hình. */
export function useViewport(): Viewport {
  const [vp, setVp] = useState<Viewport>(readViewport);

  useEffect(() => {
    const onChange = () => setVp(readViewport());
    const mqMobile = window.matchMedia('(max-width: 767px)');
    const mqPortrait = window.matchMedia('(orientation: portrait)');
    mqMobile.addEventListener('change', onChange);
    mqPortrait.addEventListener('change', onChange);
    window.addEventListener('resize', onChange);
    // Đồng bộ 1 lần phòng khi giá trị đổi giữa render đầu và mount.
    onChange();
    return () => {
      mqMobile.removeEventListener('change', onChange);
      mqPortrait.removeEventListener('change', onChange);
      window.removeEventListener('resize', onChange);
    };
  }, []);

  return vp;
}
```

- [ ] **Step 4: Chạy test để xác nhận PASS**

Run: `cd frontend && npx vitest run src/hooks/useViewport.test.ts`
Expected: PASS — 4 test xanh.

- [ ] **Step 5: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0 cả hai.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/hooks/useViewport.ts frontend/src/hooks/useViewport.test.ts
git commit -m "feat(mobile): hook useViewport phát hiện mobile/portrait

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Primitive `BottomSheet`

Tấm trượt từ đáy lên. Dùng lại cho cả drawer panel (Task 3) lẫn modal HUD mobile (Task 6). Không có test đơn vị (component hình ảnh) — nghiệm thu bằng tsc/build; hành vi kiểm tra tay ở Task 9.

**Files:**
- Create: `frontend/src/components/mobile/BottomSheet.tsx`

**Interfaces:**
- Consumes: nothing (chỉ React).
- Produces:
  - `export interface BottomSheetProps { open: boolean; onClose: () => void; title?: string; heightMode?: 'auto' | 'tall'; children: React.ReactNode }`
  - `export function BottomSheet(props: BottomSheetProps): JSX.Element | null`

- [ ] **Step 1: Viết component** — `frontend/src/components/mobile/BottomSheet.tsx`

```tsx
import { useEffect, useRef, useState } from 'react';
import { X } from 'lucide-react';

export interface BottomSheetProps {
  open: boolean;
  onClose: () => void;
  title?: string;
  /** 'auto' = cao theo nội dung; 'tall' = ~75% màn hình. */
  heightMode?: 'auto' | 'tall';
  children: React.ReactNode;
}

/**
 * Tấm trượt từ đáy màn hình (mobile). Backdrop bấm-ngoài-để-đóng, phím Escape,
 * kéo grabber xuống để đóng, khoá cuộn nền, tôn trọng safe-area (tai thỏ).
 */
export function BottomSheet({ open, onClose, title, heightMode = 'auto', children }: BottomSheetProps) {
  const [dragY, setDragY] = useState(0);
  const startY = useRef<number | null>(null);

  // Đóng bằng phím Escape.
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [open, onClose]);

  // Khoá cuộn nền khi mở.
  useEffect(() => {
    if (!open) return;
    const prev = document.body.style.overflow;
    document.body.style.overflow = 'hidden';
    return () => { document.body.style.overflow = prev; };
  }, [open]);

  // Reset vị trí kéo mỗi lần mở lại.
  useEffect(() => { if (open) setDragY(0); }, [open]);

  if (!open) return null;

  const onTouchStart = (e: React.TouchEvent) => { startY.current = e.touches[0].clientY; };
  const onTouchMove = (e: React.TouchEvent) => {
    if (startY.current === null) return;
    const dy = e.touches[0].clientY - startY.current;
    if (dy > 0) setDragY(dy);
  };
  const onTouchEnd = () => {
    if (dragY > 100) onClose();
    else setDragY(0);
    startY.current = null;
  };

  return (
    <div className="fixed inset-0 z-[60] md:hidden" role="dialog" aria-modal="true" aria-label={title || 'Bảng'}>
      {/* Backdrop */}
      <div
        className="absolute inset-0 bg-slate-950/70 backdrop-blur-sm animate-fadeIn"
        onClick={onClose}
        aria-hidden="true"
      />
      {/* Tấm sheet */}
      <div
        className={`absolute inset-x-0 bottom-0 bg-slate-900 border-t border-slate-700 rounded-t-3xl shadow-2xl flex flex-col ${
          heightMode === 'tall' ? 'h-[75vh]' : 'max-h-[75vh]'
        }`}
        style={{ transform: `translateY(${dragY}px)`, transition: startY.current === null ? 'transform 0.25s ease' : 'none', paddingBottom: 'env(safe-area-inset-bottom)' }}
      >
        {/* Grabber + header (vùng kéo) */}
        <div
          className="shrink-0 pt-2.5 pb-2 px-4 flex flex-col items-center gap-2 cursor-grab select-none"
          onTouchStart={onTouchStart}
          onTouchMove={onTouchMove}
          onTouchEnd={onTouchEnd}
        >
          <span className="w-10 h-1.5 rounded-full bg-slate-600" aria-hidden="true" />
          <div className="w-full flex items-center justify-between">
            <h3 className="text-sm font-bold uppercase tracking-wider text-slate-300">{title}</h3>
            <button
              onClick={onClose}
              aria-label="Đóng"
              className="p-2 -mr-1 rounded-lg text-slate-400 hover:text-slate-200 hover:bg-slate-800 transition"
            >
              <X size={18} aria-hidden="true" />
            </button>
          </div>
        </div>
        {/* Nội dung cuộn được */}
        <div className="flex-1 overflow-y-auto px-4 pb-4">{children}</div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0. (Nếu lỗi "JSX.Element" — đảm bảo import React types qua `React.ReactNode` như trên; project React 19 dùng automatic JSX runtime nên không cần `import React`.)

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/mobile/BottomSheet.tsx
git commit -m "feat(mobile): primitive BottomSheet (drawer trượt đáy, drag/Escape)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Thanh nav đáy + drawer panel trong App

Thêm `MobileNavBar` và nối 3 drawer (Người chơi / Tài sản / Chat) tái dùng đúng `PlayerList` / `PortfolioPanel` / `ChatPanel` sẵn có. Ẩn sidebar 260px trên mobile. Desktop giữ nguyên.

**Files:**
- Create: `frontend/src/components/mobile/MobileNavBar.tsx`
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: `useViewport` (Task 1), `BottomSheet` (Task 2).
- Produces:
  - `export type MobilePanel = 'players' | 'portfolio' | 'chat'`
  - `export interface MobileNavBarProps { active: MobilePanel | null; onOpen: (p: MobilePanel) => void }`
  - `export function MobileNavBar(props: MobileNavBarProps): JSX.Element`

- [ ] **Step 1: Viết `MobileNavBar`** — `frontend/src/components/mobile/MobileNavBar.tsx`

```tsx
import { Users, Briefcase, MessageSquare } from 'lucide-react';

export type MobilePanel = 'players' | 'portfolio' | 'chat';

export interface MobileNavBarProps {
  active: MobilePanel | null;
  onOpen: (p: MobilePanel) => void;
}

const TABS: { id: MobilePanel; label: string; Icon: typeof Users }[] = [
  { id: 'players', label: 'Người chơi', Icon: Users },
  { id: 'portfolio', label: 'Tài sản', Icon: Briefcase },
  { id: 'chat', label: 'Chat', Icon: MessageSquare },
];

/** Thanh tab dính đáy, chỉ hiện trên mobile (md:hidden). Mở drawer panel tương ứng. */
export function MobileNavBar({ active, onOpen }: MobileNavBarProps) {
  return (
    <nav
      className="md:hidden fixed inset-x-0 bottom-0 z-50 bg-slate-950/95 backdrop-blur-md border-t border-slate-800 flex items-stretch"
      style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}
      aria-label="Điều hướng bảng phụ"
    >
      {TABS.map(({ id, label, Icon }) => {
        const on = active === id;
        return (
          <button
            key={id}
            onClick={() => onOpen(id)}
            aria-label={label}
            aria-pressed={on}
            className={`flex-1 min-h-[52px] flex flex-col items-center justify-center gap-0.5 transition-colors ${
              on ? 'text-cyan-400' : 'text-slate-400 hover:text-slate-200'
            }`}
          >
            <Icon size={20} aria-hidden="true" />
            <span className="text-[10px] font-bold uppercase tracking-wider">{label}</span>
          </button>
        );
      })}
    </nav>
  );
}
```

- [ ] **Step 2: Nối vào `App.tsx` — imports + state**

Ở đầu `frontend/src/App.tsx`, thêm sau các import hiện có:

```tsx
import { useViewport } from './hooks/useViewport';
import { BottomSheet } from './components/mobile/BottomSheet';
import { MobileNavBar, type MobilePanel } from './components/mobile/MobileNavBar';
```

Trong body component `App`, ngay sau dòng `const boardThemeBg = ...`, thêm:

```tsx
  const { isMobile } = useViewport();
  const [openPanel, setOpenPanel] = useState<MobilePanel | null>(null);
  // Đóng drawer khi chuyển về desktop để tránh kẹt overlay.
  useEffect(() => { if (!isMobile) setOpenPanel(null); }, [isMobile]);
```

- [ ] **Step 3: Ẩn sidebar trên mobile**

Trong `App.tsx`, khối sidebar phải hiện là:
```tsx
<div className="w-full md:w-[260px] flex flex-col gap-4 overflow-hidden h-full shrink-0">
```
Đổi thành (thêm `hidden md:flex` để ẩn hẳn trên mobile, giữ nguyên desktop):
```tsx
<div className="hidden md:flex w-full md:w-[260px] flex-col gap-4 overflow-hidden h-full shrink-0">
```

- [ ] **Step 4: Thêm nav đáy + drawer (mobile) vào cây gameplay**

Trong `App.tsx`, ngay **trước** thẻ đóng `</div>` của khối "Màn hình Trận đấu chính (Gameplay)" (khối bắt đầu bằng `<div className="flex-1 flex flex-col overflow-hidden h-full">`), thêm:

```tsx
          {/* MOBILE: thanh tab đáy + drawer panel (tái dùng panel desktop) */}
          {isMobile && (
            <>
              <MobileNavBar active={openPanel} onOpen={(p) => setOpenPanel((cur) => (cur === p ? null : p))} />

              <BottomSheet open={openPanel === 'players'} onClose={() => setOpenPanel(null)} title="Người chơi" heightMode="tall">
                <PlayerList
                  players={visualPlayers.length > 0 ? visualPlayers : gameState.players}
                  activePlayerIndex={gameState.activePlayerIndex}
                  tiles={gameState.tiles}
                  playerId={playerId}
                />
                <button
                  onClick={() => {
                    try { localStorage.removeItem('ctp_token'); localStorage.removeItem('ctp_room'); } catch { /* ignore */ }
                    window.location.reload();
                  }}
                  className="w-full mt-4 py-3 bg-slate-800 border border-slate-700 text-slate-300 rounded-xl text-xs font-bold uppercase tracking-wider flex items-center justify-center gap-2 active:scale-[0.98]"
                >
                  <LogOut size={14} aria-hidden="true" /> Thoát ra Sảnh
                </button>
              </BottomSheet>

              <BottomSheet open={openPanel === 'portfolio'} onClose={() => setOpenPanel(null)} title="Tài sản của tôi" heightMode="tall">
                <PortfolioPanel
                  gameState={gameState}
                  playerId={playerId}
                  isMyTurn={isMyTurn}
                  buildHouse={buildHouse}
                  sellHouse={sellHouse}
                  mortgageTile={mortgageTile}
                  unmortgageTile={unmortgageTile}
                  sellDeed={sellDeed}
                  visualMoney={visualPlayers.find((p) => p.id === playerId)?.money}
                />
              </BottomSheet>

              <BottomSheet open={openPanel === 'chat'} onClose={() => setOpenPanel(null)} title="Trò chuyện" heightMode="tall">
                <ChatPanel chats={chats} sendChat={sendChat} />
              </BottomSheet>
            </>
          )}
```

- [ ] **Step 5: Chừa chỗ cho nav đáy để không che bàn**

Trong `App.tsx`, khối bàn cờ (cột trái) có class bắt đầu `flex-grow md:flex-1 ...`. Thêm padding đáy trên mobile để bàn/HUD không bị nav che: tại container layout chính
```tsx
<div className="flex-1 overflow-hidden flex flex-col md:flex-row gap-4 p-4 items-stretch justify-center w-full h-full max-w-none">
```
đổi `p-4` → `p-4 max-md:pb-20` (chừa ~80px cho nav đáy).

- [ ] **Step 6: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0. (Nếu lỗi "LogOut is not defined" — `LogOut` đã import sẵn ở `App.tsx` dòng 10; giữ nguyên.)

- [ ] **Step 7: Commit**

```bash
git add frontend/src/components/mobile/MobileNavBar.tsx frontend/src/App.tsx
git commit -m "feat(mobile): thanh tab đáy + drawer Người chơi/Tài sản/Chat

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: CSS nền (safe-area, dvh, touch-action) + meta viewport

**Files:**
- Modify: `frontend/index.html:6`
- Modify: `frontend/src/index.css` (khối `@layer base`), `frontend/src/App.tsx` (container gốc)

**Interfaces:**
- Consumes: nothing.
- Produces: không export gì — chỉ CSS/HTML nền cho các task sau dựa vào (safe-area, dvh).

- [ ] **Step 1: Thêm `viewport-fit=cover`** — `frontend/index.html` dòng 6

Đổi:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
```
thành:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
```
(Cần `viewport-fit=cover` để `env(safe-area-inset-*)` có giá trị trên máy có tai thỏ.)

- [ ] **Step 2: Thêm util nền vào `index.css`**

Trong `frontend/src/index.css`, thay khối `@layer base { ... }` hiện tại (dòng 5–9) bằng:

```css
@layer base {
  body {
    @apply bg-slate-950 text-slate-100 font-sans antialiased overflow-x-hidden;
  }
  /* Chạm: tránh double-tap zoom trên các phần tử tương tác. */
  button, a, [role="button"], input, select {
    touch-action: manipulation;
  }
}
```

- [ ] **Step 3: Dùng dynamic viewport height ở container gốc** — `frontend/src/App.tsx`

Container ngoài cùng hiện là:
```tsx
<div className="h-screen w-screen bg-slate-950 text-slate-100 flex flex-col overflow-hidden relative select-none">
```
Đổi `h-screen` → `h-[100dvh]` (fallback `h-screen` không cần thiết vì `100dvh` được các trình duyệt mobile hiện đại hỗ trợ; tránh thanh địa chỉ che đáy):
```tsx
<div className="h-[100dvh] w-screen bg-slate-950 text-slate-100 flex flex-col overflow-hidden relative select-none">
```

- [ ] **Step 4: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add frontend/index.html frontend/src/index.css frontend/src/App.tsx
git commit -m "feat(mobile): safe-area viewport-fit, dvh, touch-action nền

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Camera 3D thích ứng + guard HUD trên bàn

**Files:**
- Modify: `frontend/src/components/scene3d/GameBoard3D.tsx`

**Interfaces:**
- Consumes: `useThree` (từ `@react-three/fiber`, đã dùng gián tiếp), prop `hudContent` sẵn có.
- Produces: hành vi — khi khung dọc (cao > rộng) camera lùi xa & nghiêng để bàn vừa bề ngang; khi `hudContent` là `null` thì KHÔNG render `<Html>`.

- [ ] **Step 1: Thêm import `useThree`**

Trong `frontend/src/components/scene3d/GameBoard3D.tsx` dòng 2, đang là:
```tsx
import { Canvas, useFrame } from '@react-three/fiber';
```
Đổi thành:
```tsx
import { Canvas, useFrame, useThree } from '@react-three/fiber';
```

- [ ] **Step 2: Thêm component `ResponsiveCamera`**

Ngay **trước** `export function GameBoard3D(` (khoảng dòng 674), thêm:

```tsx
// ─── Camera thích ứng: khung dọc thì lùi xa + nghiêng để bàn 11 đơn vị vừa bề ngang ───
function ResponsiveCamera() {
  const { camera, size } = useThree();

  useEffect(() => {
    const portrait = size.height > size.width;
    // aspect = rộng / cao; càng nhỏ (màn càng dọc) càng phải lùi camera.
    const aspect = size.width / size.height;
    const persp = camera as THREE.PerspectiveCamera;

    if (portrait) {
      // Dọc: nâng cao & lùi xa; màn càng hẹp lùi càng nhiều. Nghiêng gần top-down.
      const dist = aspect < 0.55 ? 20 : 17; // rất hẹp (điện thoại) vs hẹp vừa
      persp.position.set(0, dist * 0.92, dist * 0.55);
      persp.fov = 55;
    } else {
      // Ngang/desktop: GIỮ NGUYÊN như hiện tại.
      persp.position.set(0, 10, 12);
      persp.fov = 50;
    }
    persp.updateProjectionMatrix();
  }, [camera, size.width, size.height]);

  return null;
}
```

- [ ] **Step 3: Gắn `ResponsiveCamera` vào Canvas**

Trong JSX của `GameBoard3D`, ngay sau thẻ `<Canvas ...>` mở (sau dòng `style={{ ... }}>`), thêm dòng đầu tiên trong Canvas:
```tsx
        <ResponsiveCamera />
```
(Đặt trước `{/* ─── BRIGHT Lighting ─── */}`.)

- [ ] **Step 4: Nới `OrbitControls` cho pinch-zoom thoải mái trên mobile**

Tìm khối `<OrbitControls ... maxDistance={20} ... />`. Đổi `maxDistance={20}` → `maxDistance={28}` (cho phép thu nhỏ thêm khi màn hẹp). Giữ mọi prop khác nguyên vẹn.

- [ ] **Step 5: Guard `<Html>` HUD-trên-bàn**

Tìm khối:
```tsx
        {/* ─── 5. Center HUD — FLAT on board surface ─── */}
        <Html
          center
          transform
          ...
        >
          <div className="w-[320px] pointer-events-auto">
            {hudContent}
          </div>
        </Html>
```
Bọc toàn bộ trong điều kiện `hudContent` (Board sẽ truyền `null` khi mobile):
```tsx
        {/* ─── 5. Center HUD — FLAT on board surface (chỉ desktop) ─── */}
        {hudContent && (
          <Html
            center
            transform
            position={[0, 0.08, 0]}
            rotation={[-Math.PI / 2, 0, 0]}
            scale={0.56}
            pointerEvents="auto"
            style={{ pointerEvents: 'auto' }}
          >
            <div className="w-[320px] pointer-events-auto">
              {hudContent}
            </div>
          </Html>
        )}
```

- [ ] **Step 6: Cho phép `hudContent` nhận null**

Trong interface `GameBoard3DProps` (đầu file), đổi:
```tsx
  hudContent: React.ReactNode;
```
thành:
```tsx
  hudContent: React.ReactNode | null;
```
(`React.ReactNode` vốn đã bao gồm `null`, nhưng khai báo rõ để đọc dễ.)

- [ ] **Step 7: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0. (Nếu lỗi "useEffect is not defined" — đảm bảo dòng 1 đã import `useEffect`: file hiện có `import { useRef, useMemo, useEffect } from 'react';` — giữ nguyên.)

- [ ] **Step 8: Commit**

```bash
git add frontend/src/components/scene3d/GameBoard3D.tsx
git commit -m "feat(mobile): camera 3D thích ứng khung dọc + guard HUD trên bàn

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: HUD DOM dính đáy trên mobile (Board.tsx)

Trên mobile: không truyền HUD vào bàn 3D; thay bằng thanh HUD DOM dính đáy. Modal center-stage (mua/thuê/thuế/thẻ/tù/phá sản) hiện qua `BottomSheet`. **Tái dùng** `renderCenterStageModal()` và `CenterStageControls` sẵn có → không nhân đôi logic. Phần dice-badge markup được lặp lại có kiểm soát (chấp nhận, để desktop `hudContent` không bị đụng).

**Files:**
- Modify: `frontend/src/components/Board.tsx`

**Interfaces:**
- Consumes: `useViewport` (Task 1), `BottomSheet` (Task 2), `renderCenterStageModal()` + `hudContent` + các state (`localDiceRolling`, `diceRevealActive`, `dice`, `isMyTurn`, `diceRolled`, `isAnimationDone`, `gameState`) sẵn có trong Board.
- Produces: hành vi — mobile render `renderMobileHud()`; `GameBoard3D` nhận `hudContent={isMobile ? null : hudContent}`.

- [ ] **Step 1: Thêm import `useViewport` + `BottomSheet`**

Trong `frontend/src/components/Board.tsx`, sau import `GameBoard3D` (dòng 4), thêm:
```tsx
import { useViewport } from '../hooks/useViewport';
import { BottomSheet } from './mobile/BottomSheet';
```

- [ ] **Step 2: Lấy cờ mobile trong component**

Trong `export function Board({...})`, sau dòng destructure `const { tiles, players, ... } = gameState;` (khoảng dòng 48), thêm:
```tsx
  const { isMobile } = useViewport();
```

- [ ] **Step 3: Viết `renderMobileHud()`**

Ngay **trước** `// HUD content rendered inside 3D scene` (khoảng dòng 554, chỗ khai báo `const hudContent = (`), thêm hàm:

```tsx
  // ─── HUD DOM cho mobile (dính đáy, kéo modal ra khỏi mặt bàn 3D) ───
  const renderMobileHud = () => {
    const modalOpen = !!gameState.activeModal && isAnimationDone;

    // Badge kết quả xúc xắc rút gọn cho thanh đáy.
    const diceBadge = (
      <div className="flex items-center justify-center gap-2 text-xs font-bold">
        {localDiceRolling ? (
          <span className="text-indigo-300 uppercase tracking-wider animate-pulse">🎲 Đang đổ xúc xắc...</span>
        ) : diceRevealActive ? (
          <span className="text-cyan-300 uppercase tracking-wider">🎲 {dice[0]} + {dice[1]} = {dice[0] + dice[1]}</span>
        ) : !diceRolled && isMyTurn ? (
          <span className="text-indigo-300 uppercase tracking-wider animate-pulse">🎲 Đến lượt bạn — đổ xúc xắc!</span>
        ) : diceRolled ? (
          <span className="text-slate-300 font-mono">🎲 {dice[0]} + {dice[1]} = <span className="text-emerald-400 font-black">{dice[0] + dice[1]}</span></span>
        ) : (
          <span className="text-slate-500 uppercase tracking-wider">Chờ tới lượt...</span>
        )}
      </div>
    );

    return (
      <>
        {/* Thanh HUD dính đáy (trên thanh nav 52px). Ẩn khi modal mở để nhường chỗ sheet. */}
        {!modalOpen && (
          <div
            className="md:hidden fixed inset-x-0 z-40 bg-slate-950/92 backdrop-blur-md border-t border-slate-800 px-3 pt-2.5 pb-2 flex flex-col items-center gap-2"
            style={{ bottom: 'calc(52px + env(safe-area-inset-bottom))' }}
          >
            {diceBadge}
            {isAnimationDone && !gameState.activeModal && (
              <div className="w-full max-w-[340px] flex justify-center">
                <CenterStageControls
                  isMyTurn={isMyTurn}
                  currentActionRequired={currentActionRequired}
                  diceRolled={diceRolled}
                  pendingPayment={pendingPayment}
                  winnerId={winnerId}
                  isHost={playerId === hostId}
                  inJail={!!activePlayer?.inJail}
                  hasJailCard={(activePlayer?.getOutOfJailCards || 0) > 0}
                  rollDice={rollDice}
                  buyProperty={buyProperty}
                  endTurn={endTurn}
                  declareBankruptcy={declareBankruptcy}
                  restartGame={restartGame}
                  jailAction={jailAction}
                  settleFunds={settleFunds}
                />
              </div>
            )}
          </div>
        )}

        {/* Modal center-stage → BottomSheet (tái dùng renderCenterStageModal) */}
        <BottomSheet open={modalOpen} onClose={() => { /* modal điều khiển bởi game state, không tự đóng */ }} title="Hành động">
          <div className="pb-2">{renderCenterStageModal()}</div>
        </BottomSheet>
      </>
    );
  };
```

- [ ] **Step 4: Truyền `hudContent` có điều kiện + render HUD mobile**

Ở cuối `Board`, khối return hiện là:
```tsx
  return (
    <div ref={containerRef} className="w-full h-full overflow-hidden relative select-none">
      <GameBoard3D
        gameState={gameState}
        ...
        hudContent={hudContent}
      />
    </div>
  );
```
Đổi prop `hudContent={hudContent}` → `hudContent={isMobile ? null : hudContent}`, và thêm `{isMobile && renderMobileHud()}` ngay sau `</GameBoard3D>`:
```tsx
  return (
    <div ref={containerRef} className="w-full h-full overflow-hidden relative select-none">
      <GameBoard3D
        gameState={gameState}
        playerId={playerId}
        activePlayer={activePlayer}
        buildHouse={buildHouse}
        visualPositions={visualPositions}
        boardData={boardData}
        isHighlighted={(id) => highlightedTileId === id}
        isGroupMonopoly={(group, ownerId) => isGroupMonopoly(group, ownerId)}
        getPlayersOnTile={getPlayersOnTile}
        getOwnerColor={(ownerId) => {
          const owner = players.find((p) => p.id === ownerId);
          return owner?.color;
        }}
        getCurrentRent={(tile) => {
          const tileState = tiles.find((t) => t.id === tile.id);
          if (!tileState || !tileState.ownerId) return undefined;
          if (tile.type === 'property' && tile.rent) {
            if (tileState.hotel) return tile.rent[5];
            else if (tileState.houses > 0) return tile.rent[tileState.houses];
            else {
              const isMono = isGroupMonopoly(tile.group, tileState.ownerId);
              return isMono ? tile.rent[0] * 2 : tile.rent[0];
            }
          } else if (tile.type === 'railroad') {
            const count = tiles.filter(t => t.ownerId === tileState.ownerId && boardData.find(b => b.id === t.id)?.type === 'railroad').length;
            return [25, 50, 100, 200][count - 1] || 25;
          } else if (tile.type === 'utility') {
            const count = tiles.filter(t => t.ownerId === tileState.ownerId && boardData.find(b => b.id === t.id)?.type === 'utility').length;
            return count >= 2 ? '10x' : '4x';
          }
          return undefined;
        }}
        dice={dice}
        diceRolled={diceRolled}
        isMyTurn={isMyTurn}
        localDiceRolling={localDiceRolling}
        diceRevealActive={diceRevealActive}
        isAnimationDone={isAnimationDone}
        winnerId={winnerId}
        hudContent={isMobile ? null : hudContent}
      />
      {isMobile && renderMobileHud()}
    </div>
  );
```

- [ ] **Step 5: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0. (Nếu lỗi biến chưa dùng khi mobile-only — không có, mọi biến đều dùng ở cả hai nhánh.)

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/Board.tsx
git commit -m "feat(mobile): HUD/controls/modal dạng DOM dính đáy trên mobile

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: Lobby responsive

**Files:**
- Modify: `frontend/src/components/GameLobby.tsx`

**Interfaces:**
- Consumes: nothing mới (chỉ class Tailwind responsive).
- Produces: Lobby xếp 1 cột trên mobile, input ≥16px, lưới quân tap-friendly.

> **Lưu ý cho người thực hiện:** ĐỌC `frontend/src/components/GameLobby.tsx` trước để biết cấu trúc thực tế. Các bước dưới mô tả **loại** thay đổi cần làm; áp dụng bằng cách khớp pattern hiện có, không thêm logic mới.

- [ ] **Step 1: Xếp dọc container chính trên mobile**

Tìm container lưới/hàng chính của lobby (thường là `flex ...` hoặc `grid grid-cols-2` / `md:grid-cols-...`). Đảm bảo mobile là **1 cột**: thêm/đổi để có `grid-cols-1 md:grid-cols-2` (hoặc `flex-col md:flex-row`). Giữ nhánh `md:` desktop nguyên.

- [ ] **Step 2: Input không auto-zoom trên iOS**

Với mọi `<input>` (tên người chơi, mã phòng, chat sảnh) trong file: đảm bảo có `text-base` (≥16px) — nếu đang là `text-sm`/`text-xs`, thêm `text-base md:text-sm` để mobile ≥16px còn desktop giữ nhỏ.

- [ ] **Step 3: Lưới chọn quân tap-friendly**

Tìm lưới 8 quân (skins). Đảm bảo mỗi ô chọn tối thiểu cao ~56px trên mobile (thêm `min-h-[56px]`), lưới `grid-cols-4` giữ được trên mobile (8 quân = 2 hàng). Nếu chật, dùng `grid-cols-4`.

- [ ] **Step 4: Nút hành động (Tạo/Vào/Bắt đầu) full-width + cao trên mobile**

Với các nút chính: thêm `min-h-[48px]` và `w-full md:w-auto` nếu hiện chưa full-width trên mobile.

- [ ] **Step 5: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/GameLobby.tsx
git commit -m "feat(mobile): Lobby responsive 1 cột, input ≥16px, quân tap-friendly

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: Modal phụ (Winner, Help, toast lỗi)

**Files:**
- Modify: `frontend/src/components/modals/WinnerModal.tsx`
- Modify: `frontend/src/components/HelpButton.tsx`
- Modify: `frontend/src/App.tsx` (toast lỗi)

**Interfaces:**
- Consumes: nothing mới.
- Produces: modal phụ co vừa màn nhỏ, an toàn tai thỏ.

> **Lưu ý:** ĐỌC từng file trước để khớp cấu trúc thực tế; chỉ chỉnh class responsive, không đổi logic.

- [ ] **Step 1: WinnerModal co theo màn**

Trong `WinnerModal.tsx`: card thắng đảm bảo `w-[92vw] max-w-md` (thay vì cố định rộng), thân cuộn được nếu dài: thêm `max-h-[85vh] overflow-y-auto`, padding `p-4 md:p-6`. Nút hành động `min-h-[48px]`.

- [ ] **Step 2: Help modal full-width mobile**

Trong `HelpButton.tsx`: panel/modal trợ giúp đảm bảo `w-[92vw] max-w-lg max-h-[85vh] overflow-y-auto`, `p-4 md:p-6`. (Escape đã có sẵn — giữ.)

- [ ] **Step 3: Toast lỗi không đè nút đáy + vừa màn**

Trong `App.tsx`, toast lỗi hiện có class `fixed top-6 right-6 z-50 max-w-sm ...`. Đổi thành `fixed top-4 right-4 left-4 md:left-auto z-[70] max-w-sm md:max-w-sm ...` để trên mobile toast trải ngang vừa màn ở trên cùng (không đụng nav đáy). Giữ desktop gần như cũ (`md:left-auto` trả về góc phải).

- [ ] **Step 4: Verify tsc + build**

Run: `cd frontend && npx tsc -b && npx vite build`
Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/modals/WinnerModal.tsx frontend/src/components/HelpButton.tsx frontend/src/App.tsx
git commit -m "feat(mobile): WinnerModal/Help/toast co vừa màn nhỏ + safe-area

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: Kiểm thử tay + landscape + verify tổng

Không viết code mới trừ khi phát hiện lỗi. Đây là cổng nghiệm thu cuối.

**Files:**
- Modify: chỉ khi tìm thấy lỗi trong lúc test tay.

- [ ] **Step 1: Verify build + toàn bộ test**

Run:
```bash
cd frontend && npx tsc -b && npx vite build && npx vitest run
```
Expected: `tsc -b` exit 0; `vite build` exit 0; vitest — mọi test cũ xanh + `useViewport` xanh.

- [ ] **Step 2: Chạy dev server**

Run (backend + frontend, xem `docs/PROGRESS.md` §Cách chạy):
```bash
cd backend && npm run dev    # cổng 5000 (chạy nền)
cd frontend && npm run dev   # cổng 5173
```

- [ ] **Step 3: Test tay — điện thoại DỌC (Chrome DevTools → Device toolbar → iPhone 14)**

Kịch bản, xác nhận từng mục PASS:
- [ ] Sảnh: nhập tên/mã phòng KHÔNG bị trình duyệt auto-zoom; chọn quân dễ chạm; tạo/vào phòng được; layout 1 cột không tràn ngang.
- [ ] Vào trận: bàn 3D **vừa khít bề ngang**, không tràn/cắt; vuốt 1 ngón xoay được, pinch zoom được.
- [ ] Thanh HUD đáy: badge xúc xắc + nút "Đổ xúc xắc" hiện rõ, bấm được 1 tay.
- [ ] Đổ xúc xắc → quân đi → đáp ô mua được: **BottomSheet "Hành động"** trượt lên với thẻ mua; nút Mua/Bỏ qua ≥44px.
- [ ] Trả thuê / nộp thuế / rút thẻ / vào tù: mỗi modal hiện qua BottomSheet, nút Xác nhận chạm được.
- [ ] Mở lần lượt drawer Người chơi / Tài sản / Chat: trượt lên đúng nội dung; kéo grabber xuống đóng được; bấm backdrop đóng; nút X đóng; Escape đóng.
- [ ] Gửi chat trong drawer Chat được; input không auto-zoom.
- [ ] BuildModal khi đáp đất mình (nếu đủ điều kiện xây) hiện & bấm được.
- [ ] Nav đáy KHÔNG che bàn/HUD; không phần tử nào bị tai thỏ cắt.

- [ ] **Step 4: Test tay — điện thoại NGANG (xoay Device toolbar sang landscape)**

- [ ] Nếu khung ≥768px (đa số điện thoại landscape): rơi vào nhánh desktop (bàn giữa + sidebar) — kiểm tra không vỡ, bàn dùng camera "rộng" `[0,10,12]`.
- [ ] Nếu khung <768px: nhánh mobile (nav đáy + drawer), camera "rộng"; kiểm tra không tràn.
- [ ] Xoay qua lại dọc↔ngang vài lần: camera cập nhật mượt, không kẹt overlay.

- [ ] **Step 5: Test tay — DESKTOP (thu Device toolbar, cửa sổ ≥768px)**

- [ ] So với trước khi làm task: bàn 3D + HUD-trên-bàn + sidebar 260px + chat/portfolio **y hệt** (không có nav đáy, không drawer). ✅ Bất biến desktop.

- [ ] **Step 6: (Tùy chọn) Sửa lỗi phát sinh**

Nếu Step 3–5 lộ lỗi: sửa tối thiểu, verify lại `tsc -b`+`vite build`, commit riêng với message `fix(mobile): <mô tả>`.

- [ ] **Step 7: Cập nhật PROGRESS.md**

Trong `docs/PROGRESS.md`, mục "VIỆC TIẾP THEO", đánh dấu mục 2 (Responsive/mobile T4.4) là **ĐÃ XONG**; thêm 1 dòng tóm tắt vào phần "ĐÃ XONG THÊM" với ngày 2026-07-02, dẫn spec `docs/specs/2026-07-02-mobile-ui-design.md`.

- [ ] **Step 8: Commit**

```bash
git add docs/PROGRESS.md
git commit -m "docs: đánh dấu T4.4 mobile UI hoàn thành

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Ghi chú cho người thực hiện

- **Thứ tự bắt buộc:** Task 1 → 9 tuần tự (Task 3 cần 1&2; Task 6 cần 1,2,5). Không nhảy cóc.
- **Sau mỗi task:** phải xanh `tsc -b` + `vite build` mới commit.
- **Desktop là thiêng liêng:** nếu bất kỳ thay đổi nào làm desktop khác đi so với trước, đó là lỗi — sửa để mobile-only.
- **Nếu code thực tế trong file lệch với đoạn trích trong plan** (do commit mới): khớp theo *ý định* (ẩn trên mobile / thêm nhánh mobile / guard null), không phá logic hiện có.
